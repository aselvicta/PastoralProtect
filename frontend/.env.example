# Copy to .env.local (never commit .env.local). Adjust when the API is not on localhost.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# Optional: override demo quick sign-in — must match backend SEED_JUDGE_DEMO_PASSWORD in backend/.env
# NEXT_PUBLIC_DEMO_QUICK_LOGIN_USERNAME=
# NEXT_PUBLIC_DEMO_QUICK_LOGIN_PASSWORD=
