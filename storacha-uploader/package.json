{
  "name": "pastoral-protect-storacha-uploader",
  "version": "1.0.0",
  "private": true,
  "description": "Small HTTP bridge: FastAPI uploads JSON via @storacha/client (email auth, no legacy API keys).",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "login": "node login.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@storacha/client": "^2.1.3",
    "dotenv": "^16.4.5",
    "express": "^5.2.1"
  }
}
